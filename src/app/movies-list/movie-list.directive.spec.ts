import { MovieListDirective } from './movie-list.directive';

describe('MovieListDirective', () => {
  it('should create an instance', () => {
    const directive = new MovieListDirective();
    expect(directive).toBeTruthy();
  });
});
